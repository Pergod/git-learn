now let start to learn git!
First class:
	git init
	git add (fileName)
	git commit -m "some message"
Second class 
	git status
	git diff
Third class
	git log [--pretty=oneline]
	git reset --hard head^
	cat (fileName)